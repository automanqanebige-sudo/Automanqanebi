/** This repo — new deployment target (legacy live site may remain on automanqanebi.ge). */
export const SITE_DOMAIN = 'automanqanebi1.ge' as const

export const SITE_URL = `https://${SITE_DOMAIN}` as const

/** Navbar / footer logo text */
export const SITE_LOGO_MAIN = 'AUTOMANQANEBI1' as const
export const SITE_LOGO_TLD = '.GE' as const

export const SITE_CONTACT_EMAIL = `info@${SITE_DOMAIN}` as const
